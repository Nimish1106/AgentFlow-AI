fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7,<3.0
pydantic-settings>=2.3
redis>=5.0
httpx>=0.27
faker>=25.0

# LangGraph orchestration + Groq LLM (SRS §8)
langgraph>=1.2,<2.0
langchain-core>=1.0,<2.0
langchain-groq>=1.1,<2.0
groq>=0.30

# Postgres-backed LangGraph checkpointer (SRS §16.9: workflows are resumable).
# The saver uses psycopg (v3), separate from the app's asyncpg engine.
langgraph-checkpoint-postgres>=3.1,<4.0
psycopg[binary,pool]>=3.2,<4.0

# Enterprise MCP Server + client (SRS §31)
mcp>=1.29,<2.0

# RAG: Qdrant vector store + local ONNX embeddings (SRS §8, §32)
qdrant-client>=1.12,<2.0
fastembed>=0.5,<1.0

# Observability (SRS §42). The API and SDK are stable 1.x; the instrumentation
# line is versioned 0.x/bN by upstream convention and must stay in lockstep with
# the SDK release it was built against.
opentelemetry-api>=1.44,<2.0
opentelemetry-sdk>=1.44,<2.0
opentelemetry-exporter-otlp-proto-http>=1.44,<2.0
opentelemetry-instrumentation-fastapi==0.65b0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
aiosqlite>=0.20
